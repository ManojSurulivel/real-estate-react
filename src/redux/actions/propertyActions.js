export const setProperties = (properties) => ({
    type: 'SET_PROPERTIES',
    payload: properties,
  });
  
  export const filterProperties = (filters) => ({
    type: 'FILTER_PROPERTIES',
    payload: filters,
  });
  